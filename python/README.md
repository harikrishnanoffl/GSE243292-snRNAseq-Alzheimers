# Python conversion

Place `01_convert_h5ad.py` in `D:\nsg consultaion\python\`.

Run from the project root with:

```powershell
python python\01_convert_h5ad.py
```

The script reads the existing GSE243292 H5AD and writes sparse Seurat-compatible files under `data\processed\`. Python is not used for the biological analysis.
